[project]
name = "build-buddy"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "google-adk>=1.26.0",
    "google-cloud-firestore>=2.25.0",
    "google-genai>=1.65.0",
]

[tool.pyright]
venvPath = "."
venv = ".venv"
